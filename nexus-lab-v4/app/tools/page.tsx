import type { Metadata } from "next";
import ToolsClient from "../components/ToolsClient";
export const metadata: Metadata = { title:"Tool Hub — NEXUS LAB", description:"Six local-first creative generators for prompts, gradients, neon, glass, color and motion." };
export default function Page(){return <ToolsClient/>}
